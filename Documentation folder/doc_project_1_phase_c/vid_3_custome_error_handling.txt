here we will be dealing with custome error handling 

definging our own customes

generally we use it to deal with asynchronous errors because these errers 

to handle this error we define our middlewares

inside the app.js


----
//create Route
app.post("/listings", async (req,res,next)=>{

    try{
//let listing = req.body.listing;
const newListing = new Listing(req.body.listing);

 await newListing.save()
 res.redirect("/listings");
 }catch(err){
    next(err)
 }
})

// error handler
app.use((err,req,res,next)=>{
    res.send("somthing went wrong")
})