in previous step we collected review thorough form now we will add the review inside the reviews database and add its id in listings schema 


so for that we have to create a route of listings/:id/review --> added at forms action

and connect it to the form and save the data in databse of both listings as well as rieviews

code in show.ejs

<%layout("/layouts/boilerplate")%>

<body>
  <div class="row mt-3">
    <div class="col-8 offset-3">
      <h3><%listing.title%></h3>
    </div>

    <div class="card col-6 offset-3 show-card">
      <!-- copies design from bootstrap -->
      <img
        src="<%= listing.img %>"
        class="card-img-top show-img"
        alt="listing image"
      />
      <div class="card-body">
        <p class="card-text">
          <%=listing.description%>
          <br />
          &#8377;<%=listing.price.toLocaleString("en-IN")%>
          <br />
          <%=listing.location%>
          <br />
          <%=listing.country%>
        </p>
      </div>
    </div>
    <div class="btns mb-3 mt-3">
      <a
        href="/listings/<%=listing._id%>/edit"
        class="btn btn-dark cal-1 offset-3 edit-btn"
        >Edit
      </a>
      <br />
      <form action="/listings/<%=listing._id%>?_method=DELETE" method="post">
        <button class="btn btn-dark offset-5">Delete</button>
      </form>
    </div>
    <hr />
    <div class="row mb-3 mt-3 col-8 offset-3">
      <h4>leave a review</h4>
      <form action="/listings/<%=listing._id %>/reviews" method="post">
        <div>
          <input
            type="range"
            name="review[rating]"
            id="rating"
            class="form-range"
            range="5"
            min="1"
            max="5"
          />
        </div>
        <div class="mb-3">
          <label for="comment" class="form-label"></label>
          <textarea
           
            name="review[comment]"
            rows="10"
            cols="5"
            id="review"
            class="form-control"
          ></textarea>
        </div>
        <button class="btn btn-dark">submit</button>
      </form>
    </div>
  </div>
</body>


// creating route for collecting the review

app.post("/listings/:id/reviews", async(req,res)=>{
    let listing = await Listing.findById(req.params.id);
    let newReview = new Review(req.body.review);

    listing.reviews.push(newReview);

    await listing.save()
    await newReview.save()

    res.redirect(`/listings/${listing._id}`)

})


I was making so many errors while doing this portion of the code so be concious and understand it again and again...
ydfuyfu